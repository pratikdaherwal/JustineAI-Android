<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.JustineAI" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="gradientBackground">@drawable/background_gradient_light</item>
        <item name="editTextBackground">@color/white</item>
        <item name="strokeColor">@color/purple</item>
        <item name="splashColor">@color/black</item>
    </style>

    <style name="Theme.JustineAI" parent="Base.Theme.JustineAI" />

    <style name="Theme.JustineAI.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>